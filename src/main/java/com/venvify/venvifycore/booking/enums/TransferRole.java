package com.venvify.venvifycore.booking.enums;

/** Vai của user trong list transfer của mình (plan P3 §1.5): SENT = người gửi, RECEIVED = người nhận. */
public enum TransferRole {
    SENT,
    RECEIVED
}
