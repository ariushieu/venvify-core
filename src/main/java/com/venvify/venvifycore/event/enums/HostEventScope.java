package com.venvify.venvifycore.event.enums;

/** Tab event trên storefront host (plan P3 §2.4): UPCOMING = PUBLISHED chưa tới giờ; PAST = ENDED. */
public enum HostEventScope {
    UPCOMING,
    PAST
}
