server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  application:
    name: venvify-core

  datasource:
    # Prod (Docker) set SPRING_DATASOURCE_URL trỏ host `mysql`; dev mặc định localhost.
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/venvify_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      # Flyway sở hữu schema; Hibernate chỉ đối chiếu entity ↔ schema, KHÔNG tự sửa.
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          # T1 (architecture §15): UTC toàn tuyến — DB lưu UTC, client tự render giờ treo tường
          # theo events.timezone. Đổi múi giờ hiển thị là việc của FE, không phải của storage.
          time_zone: UTC
        # Enum policy (architecture §6): từ V5 mọi cột enum là VARCHAR(30). KHÔNG cần config —
        # validator MySQLDialect coi ENUM/VARCHAR tương đương (events.timezone enum↔varchar
        # đã pass validate từ V2); property preferred_enum_jdbc_type không tồn tại trong
        # Hibernate 7.4.1. DDL bảng mới viết tay trong Flyway → luôn dùng VARCHAR.

  flyway:
    enabled: true
    baseline-on-migrate: false
    locations: classpath:db/migration

  jackson:
    time-zone: UTC  # T1: JSON timestamps ISO-8601 UTC (khớp hibernate.jdbc.time_zone)

logging:
  config: classpath:log4j2.xml

# Auth (JWT HS256 khóa chia sẻ — SPEC §5.2). Secret bắt buộc từ env, KHÔNG hard-code.
jwt:
  secret: ${SECRET_KEY}
  access-token-expiration: 900000      # 15 phút (ms)
  refresh-token-expiration: 604800000  # 7 ngày (ms)

# Gửi email qua Resend.
resend:
  api-key: ${RESEND_API_KEY}

app:
  money:
    ticket-commission-percent: 5      # O-M1: phí platform, floor khi chia (R17); đổi không cần migration
    escrow-release-delay-days: 3      # O-M3: dispute window sau khi event ENDED mới release escrow
    dev-topup-enabled: false          # bật tay ở local để test paid flow; prod LUÔN tắt bất kể flag (R16)
  auth:
    # T6: chống brute-force login — khóa account sau N lần sai trong cửa sổ lockout.
    # Response vẫn 401 generic, không lộ trạng thái khóa.
    login-max-failures: 10
    login-lockout-minutes: 15
  cors:
    # CSV origin FE được phép gọi API. Override bằng env APP_CORS_ALLOWED_ORIGINS ở prod.
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  mail:
    from: ${RESEND_FROM_EMAIL:onboarding@resend.dev}
    verification-token-expiration: 600000  # OTP hiệu lực 10 phút (ms)
  ops:
    # Nhận email P0 (reconcile lệch tiền — money-core §4). Để trống = chỉ log ERROR (dev).
    admin-email: ${ADMIN_ALERT_EMAIL:}

---
# Profile PROD (Docker/VPS): tắt log SQL cho gọn log & đỡ tốn I/O.
# Mọi secret/URL còn lại đã đọc từ env (SPRING_DATASOURCE_URL, SECRET_KEY, ...) — set trong .env của compose.
spring:
  config:
    activate:
      on-profile: prod
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

---
# Profile CHẠY MỘT LẦN để export DDL từ entity ra V1__init_schema.sql.
# Dùng: ./mvnw spring-boot:run -Dspring-boot.run.profiles=schema-gen
# Sau khi có file V1, không cần profile này nữa.
spring:
  config:
    activate:
      on-profile: schema-gen
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      jakarta:
        persistence:
          schema-generation:
            scripts:
              action: create
              create-source: metadata
              create-target: src/main/resources/db/migration/V1__init_schema.sql
  flyway:
    enabled: false